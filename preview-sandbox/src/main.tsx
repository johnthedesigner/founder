import React from 'react'
import ReactDOM from 'react-dom/client'
import { TokenApplicator } from './TokenApplicator'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <TokenApplicator />
  </React.StrictMode>,
)
